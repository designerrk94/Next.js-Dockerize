name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install

    - name: sudo Build Next.js app
      run: npm run build

    - name: sudo Build Docker image
      run: docker build -t my-next-app .

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Push Docker image
      run: |
        docker tag my-next-app YOUR_DOCKERHUB_USERNAME/my-next-app:latest
        docker push YOUR_DOCKERHUB_USERNAME/my-next-app:latest

